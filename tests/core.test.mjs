import test from 'node:test';import assert from 'node:assert/strict';import {parseRoads,nearestNode,junctionOptions,outcome,geoBounds,relativeTurn} from '../src/core.js';
const o={lat:40.33,lon:-3.89};const data={elements:[{type:'node',id:1,lat:40.33,lon:-3.89},{type:'node',id:2,lat:40.3301,lon:-3.89},{type:'node',id:3,lat:40.3302,lon:-3.89},{type:'node',id:4,lat:40.3301,lon:-3.8901},{type:'way',id:10,nodes:[1,2,3],tags:{highway:'residential',name:'Calle Ejemplo'}},{type:'way',id:11,nodes:[2,4],tags:{highway:'residential',oneway:'yes'}}]};
test('construye red y respeta sentidos',()=>{const g=parseRoads(data,o);assert.equal(g.paths.length,2);assert.equal(g.edges.get(4),undefined);assert.equal(junctionOptions(g,2,1).length,2);assert.equal(nearestNode(g,o).id,1);});
test('sin marcha atrás en bifurcación',()=>{const g=parseRoads(data,o);assert.ok(junctionOptions(g,2,1).every(x=>x.to!==1));});
test('contabiliza resultado simulado',()=>{assert.equal(outcome([{code:'D'},{code:'L'},{code:'L'},{code:'L'},{code:'L'},{code:'L'}]).passed,false);assert.equal(outcome([{code:'E'}]).passed,false);assert.equal(outcome([{code:'L'}]).passed,true);});
test('coordenadas y giros',()=>{assert.equal(geoBounds(40,-3,.01),'39.990000,-3.010000,40.010000,-2.990000');assert.equal(relativeTurn({x:0,z:0},{x:0,z:1},{x:1,z:1}),'izquierda');assert.equal(relativeTurn({x:0,z:0},{x:0,z:-1},{x:1,z:-1}),'derecha');});
